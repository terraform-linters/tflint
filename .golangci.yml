run:
  timeout: 5m
  skip-dirs:
    - terraform/
linters:
  enable:
    - gci
    - gofmt
    - misspell
    - revive
    - unconvert
    - unparam
issues:
  exclude-rules:
    - path: terraform/
      linters:
        - gosimple
